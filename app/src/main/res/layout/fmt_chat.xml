<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/fmt_chat_rv_chat"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="0.9"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_weight="0.1">

        <EditText
            android:id="@+id/fmt_chat_et_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/internal_padding"
            android:layout_weight="0.9"/>

        <ImageView
            android:id="@+id/fmt_chat_iv_send"
            android:src="@drawable/ic_send"
            android:layout_gravity="center"
            android:layout_marginTop="@dimen/internal_padding"
            android:layout_marginLeft="@dimen/internal_padding"
            android:layout_marginRight="@dimen/internal_padding"
            android:layout_marginBottom="@dimen/external_padding"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="0.1"/>
    </LinearLayout>
</LinearLayout>